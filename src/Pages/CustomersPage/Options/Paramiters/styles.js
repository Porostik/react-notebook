import { makeStyles } from '@material-ui/core';

const styles = makeStyles((theme) => ({
  checkbox: {
    ...theme.checkbox,
  },
  paramText: {
    color: theme.color,
  },
}));

export default styles;

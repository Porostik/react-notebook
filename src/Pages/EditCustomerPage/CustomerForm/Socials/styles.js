import { makeStyles } from '@material-ui/core';

const styles = makeStyles((theme) => ({
  root: {},
  button: {
    ...theme.button,
  },
  socialsPlaceholder: {
    color: theme.color,
  },
}));

export default styles;
